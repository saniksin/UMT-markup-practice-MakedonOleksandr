VITE_API_BASE_URL="/api"
# For GitHub Pages / static hosting: emit dist/api/*.json and fetch catalogue from files.
# Set in CI (see .github/workflows/deploy.yml). For local json-server, omit this.
# VITE_API_MODE=static
